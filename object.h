#include <iostream>

class object{
    public:
    int itemNum;
    std::string size;
    std::string customerName;

    object();
    object(int itemNum, std::string size, std::string customerName);
};